<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transacción Exitosa</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
        }

        .success-icon {
            color: #ff0000;
            font-size: 48px;
        }

        .success-message {
            color: #ff0000;
            font-size: 24px;
            font-weight: bold;
            margin-top: 10px;
        }

        .redirect-message {
            color: #888;
            margin-top: 10px;
        }

        a {
            color: #0275d8;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
    <link rel="stylesheet" href="">
</head>
<body>
    <div class="container">
        <div class="success-icon">&#10006;</div>
        <div class="success-message">
            ¡La transacción fue rechazada!
        </div>
        <div class="redirect-message">
            Serás redirigido en <span id="countdown">5</span> segundos. Si no eres redirigido, haz clic <a href="/">aquí</a>.
        </div>
    </div>

    <script>
        // Redirigir después de 5 segundos
        setTimeout(function() {
            window.location.href = "/";
        }, 5000);

        // Actualizar el contador regresivo
        var seconds = 5;
        setInterval(function() {
            seconds--;
            document.getElementById("countdown").textContent = seconds;
        }, 1000);
    </script>
</body>
</html>
