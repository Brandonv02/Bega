const dbprod = require("../../../../backend/models/products.model")

function datosprod("codigo","descripcion","tipo","valor","stock") {
  idcodigo.value = codigo,
  iddescripcion.value = descripcion,
  idtipo.value = tipo,
  idvalor.value = valor,
  idstock.value = stock
};

module.exports = {
  prod: prod,
}
